<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jobify - Find Your Perfect Match</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/index.css">
</head>
<body>

  <header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">Jobify</a>
       
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/joblogin">For Job Seekers</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/recuriterlogin">For Recruiters</a>
            </li>
            <!-- Post More Jobs button in navbar -->
            <% if(role == 'recruiter'){ %> 
              <li class="nav-item">
              <a href="/jobposting" class="nav-link btn btn-primary">Post More Jobs</a>
            </li>
            <li class="nav-item">
              <a href="/logout" class="nav-link btn btn-danger">Logout</a>
            </li>
            <% } %>
         <% if(role == 'jobseeker' ){ %>
          <li class="nav-item">
            <a href="/logout" class="nav-link btn btn-danger">Logout</a>
          </li>
            <% }  %>
          </ul>
        </div>
      </div>
    </nav>
  </header>

  <div class="video-background">
    <video autoplay muted loop>
      <source src="/videos/background.mp4" type="video/mp4">
    </video>
  </div>

<main>
  <!-- Your main content goes here -->
  <%- body %>
</main>

<!-- Loader script -->
<script>
    // Create the loader element
    const loader = document.createElement('div');
    loader.id = 'loader';
    loader.style.position = 'fixed';
    loader.style.left = '50%';
    loader.style.top = '50%';
    loader.style.width = '80px';
    loader.style.height = '80px';
    loader.style.margin = '-40px 0 0 -40px';
    loader.style.border = '8px solid #f3f3f3';
    loader.style.borderTop = '8px solid #3498db';
    loader.style.borderRadius = '50%';
    loader.style.animation = 'spin 2s linear infinite';
    loader.style.zIndex = '9999';

    // Append the loader to the body
    document.body.appendChild(loader);

    // Define the keyframes for the spin animation
    const styleSheet = document.createElement('style');
    styleSheet.type = 'text/css';
    styleSheet.innerText = `
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    `;
    document.head.appendChild(styleSheet);

    // Function to remove the loader after content is loaded
    window.addEventListener('load', () => {
        loader.style.display = 'none';
    });
</script>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlDktKR6c5t8KXkYuSlC9Sjp4yl2QIv4+KI5J5W/soc59+P1m"></script>
</body>
</html>
